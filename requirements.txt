# 运行时依赖
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
httpx>=0.27
python-dotenv>=1.0

# 可选：LangChain 调用链（pip install -e ".[ai]"）
# langchain-core>=0.2
# langchain-openai>=0.1

# 开发与测试（小团队可一并安装；若仅需部署运行时可只装以上几行）
pytest>=8.0
ruff>=0.3
