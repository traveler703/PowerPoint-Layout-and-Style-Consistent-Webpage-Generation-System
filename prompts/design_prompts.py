"""
设计提示词 - LandPPT原版核心
步骤5: 提取设计基因
步骤6: 生成全局宪法
步骤7: 生成单页HTML
"""

STYLE_GENES_PROMPT = """分析以下HTML模板，提取核心设计基因用于保持风格一致性。

**模板HTML**：
{template_html}

**输出要求**：
请提取并输出以下设计基因，每项用一行描述：

1. **核心色彩**：
   - 主色（背景/大面积使用）
   - 辅色（次要元素）
   - 强调色（重点突出）
   - 文字色系

2. **字体系统**：
   - 标题字体（family + 粗细）
   - 正文字体（family + 大小）
   - 特殊字体（如数字/英文装饰）

3. **布局方式**：
   - 整体布局（Flex/Grid/其他）
   - 栅格系统（几栏几列）
   - 间距规范

4. **设计元素**：
   - 圆角样式（统一还是有层次）
   - 阴影效果（有无、颜色、模糊度）
   - 渐变使用（背景/按钮/装饰）
   - 边框风格

5. **装饰风格**：
   - 是否有装饰线条/图形
   - 图标风格（线性/填充）
   - 背景纹理/图案

请用简洁的描述性语言输出，便于后续AI理解和复用。
"""

CONSTITUTION_PROMPT = """基于以下信息生成全局设计宪法，用于指导整份PPT的生成。

**基本信息**：
- 主题：{topic}
- 场景：{scenario}
- 风格：{style}
- 总页数：{page_count}
- 设计基因：{style_genes}

**大纲摘要**：
{slides_summary}

**宪法必须包含**：

1. **内容深度策略**：
   - 封面页：如何吸引注意力
   - 内容页：信息密度规划（每页核心点数量）
   - 结论页：如何强化记忆点
   - 整体内容丰富度标准

2. **视觉节奏规划**：
   - 封面到目录：如何过渡
   - 内容部分：如何避免单调（交替使用不同版式）
   - 章节过渡：使用章节分隔页的时机
   - 结论部分：如何收尾

3. **装饰元素使用原则**：
   - 何时使用图表/数据可视化
   - 何时使用图标
   - 何时使用色块/卡片
   - 何时使用背景装饰
   - 如何平衡文字与视觉元素

4. **排版规范**：
   - 标题/正文/注释的字号比例
   - 行高与间距规范
   - 留白比例标准
   - 文字行数限制（单页最多几行）

5. **一致性保证**：
   - 每页必须保持的共同元素
   - 颜色使用的一致性规则
   - 字体使用的一致性规则

请用结构化的文本输出，便于AI在生成每页时引用。
"""

DESIGN_PROMPT = """你是一位专业的PPT设计师，请根据以下信息生成单页HTML。

**当前页任务**：
- 页码：{page_number}/{total_pages}
- 类型：{slide_type}
- 标题：{title}
- 内容要点：{content_points}

**设计基因**（必须严格遵守）：
{style_genes}

**全局宪法**（必须严格遵守）：
{global_constitution}

**固定画布约束**：
- 画布尺寸：1280px × 720px (16:9)
- 三段式结构：Header(80px) + Main(580px) + Footer(60px)
- 禁止出现滚动条
- 禁止内容超出画布边界
- 禁止使用text-overflow截断正文

**版式方法库**：
{layout_methods}

**生成规则**：
1. 主内容区必须围绕当前页任务重新组织
2. 标题区、页码区必须继承模板的层级和位置
3. 避免四宫格均质化布局
4. 优先建立视觉层次，再考虑装饰
5. 内容要丰富具体，避免空洞

**自检清单**：
- [ ] 是否超出1280x720边界？
- [ ] 主内容区是否重新组织？
- [ ] 锚点区（标题、页码）是否正确？
- [ ] 内容是否丰富具体？
- [ ] 视觉层次是否清晰？

请只返回HTML代码块，不要包含任何解释。
```html
<!DOCTYPE html>
<html>
...
</html>
```
"""
